# Heart Disease – ML Full Pipeline

End-to-end pipeline on UCI Heart Disease: EDA → PCA → Feature Selection → Supervised (baseline & tuning) → Unsupervised → Streamlit.

## Quick start (Windows)
```bash
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt
